[package]
name = "telekinesis-gui"
version = "0.2.5"
edition = "2021"
license = "MPL-2.0"
description = "Telekinesis companion — floating GPUI overlay for general computer use, powered by rx4"
repository = "https://github.com/tschk/telekinesis"
keywords = ["ai", "agent", "gui", "computer-use", "rx4"]
categories = ["gui", "development-tools"]

[[bin]]
name = "telekinesis-companion"
path = "src/main.rs"

[features]
default = ["macos"]
macos = ["crepuscularity-gpui/macos"]
x11 = ["crepuscularity-gpui/x11"]
wayland = ["crepuscularity-gpui/wayland"]
windows = ["crepuscularity-gpui/windows"]

[dependencies]
crepuscularity-gpui = { version = "0.5.8", path = "../../../crepuscularity/crates/crepuscularity-gpui", default-features = false, features = ["font-kit", "symbols"] }
crepuscularity_macros = { version = "0.5.11", path = "../../../crepuscularity/crates/crepuscularity_macros" }
gpui = "=0.2.2"
rx4 = { version = "0.6.4", default-features = false, features = ["providers", "builtin-tools", "computer-use"] }
rs_ai_oauth = "0.2.31"
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "sync"] }
async-trait = "0.1"
futures = "0.3"
tray-icon = "0.24"
global-hotkey = "0.8"

[target.'cfg(target_os = "macos")'.dependencies]
objc2 = "0.6"
objc2-app-kit = { version = "0.3", features = [
    "NSApplication",
    "NSView",
    "NSWindow",
] }
objc2-core-foundation = "0.3"
objc2-foundation = { version = "0.3", features = ["NSString"] }
raw-window-handle = "0.6"
